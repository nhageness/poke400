# Debug/spool file captures from pub400
PUB400.COM/

# Claude Code
CLAUDE.md
.claude/

# Deploy scripts (contain credentials)
pub400_deploy.js
pub400_dspf.js
pub400_menu.js
pub400_run_sql.js
node_modules/
package.json
package-lock.json

# Local-only (not shared to GitHub)
tools/
docs/

# Local SQL migration log — one-off scripts already applied to pub400.
# Final schema/seed state lives in pokemon_battle_engine.sql + seed_moves.sql.
work.sql
